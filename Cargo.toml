[workspace]
resolver = "3"

members = ["dedicated_server", "gatekeeper", "orchestrator", "shared", "game_sockets", "broker", "spatial_service"]

[workspace.dependencies]
shared = { path = "shared"}
game_sockets = { path = "game_sockets" }
